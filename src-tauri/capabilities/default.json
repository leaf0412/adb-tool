{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:default",
    "shell:allow-open",
    {
      "identifier": "shell:allow-execute",
      "allow": [{ "name": "adb", "sidecar": true, "args": true }]
    },
    "shell:allow-stdin-write",
    {
      "identifier": "shell:allow-spawn",
      "allow": [{ "name": "adb", "sidecar": true, "args": true }]
    },
    "shell:allow-kill",
    "dialog:default"
  ]
}
